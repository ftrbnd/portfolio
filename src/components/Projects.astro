---
import { favorites } from "../projects/favorites";
import Project from "./Project";
---

<div class="p-8">
    <h3 class="text-2xl font-bold pb-4">/ Projects</h3>

    <div class="flex items-center justify-center flex-col lg:flex-row gap-3">
        {favorites.map(project => <Project {...project} client:only="solid-js" />)}
    </div>
</div>